# Although the input file is one of the arguments
# for the qc command, place it in the
# config_snakemake_bash.yaml file when using the pipeline
--n_genes_by_counts=9000
--pct_counts_mt=15
--exclude_highly_expressed=yes
--show=no
